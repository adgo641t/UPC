import { RepitePassDirective } from './repite-pass.directive';

describe('RepitePassDirective', () => {
  it('should create an instance', () => {
    const directive = new RepitePassDirective();
    expect(directive).toBeTruthy();
  });
});
